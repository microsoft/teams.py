[project]
name = "common"
version = "0.0.0"
description = "Add your description here"
readme = "README.md"
repository = "https://github.com/microsoft/teams.py"
keywords = ["microsoft", "teams", "ai", "bot", "agents"] 
license = "MIT"
authors = [
    { name = "Microsoft", email = "teams@microsoft.com" }
]
requires-python = ">=3.12"
dependencies = [
    "coverage>=7.8.0",
    "pytest>=8.3.5",
    "ruff>=0.11.5",
]

[project.urls]
Homepage = "https://github.com/microsoft/teams.py/tree/main/packages/common"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["common"]

[dependency-groups]
dev = []
