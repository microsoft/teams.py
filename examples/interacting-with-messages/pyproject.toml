[project]
name = "interacting-with-messages"
version = "0.1.0"
description = "Demonstrates quoting, threading, and reactions between Teams messages"
readme = "README.md"
requires-python = ">=3.11,<4.0"
dependencies = [
    "dotenv>=0.9.9",
    "microsoft-teams-apps",
    "microsoft-teams-api",
]

[tool.uv.sources]
microsoft-teams-apps = { workspace = true }
